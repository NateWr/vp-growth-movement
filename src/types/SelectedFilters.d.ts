export type SelectedFilters = {
  area?: string[],
  campaign?: string[],
  country?: string[],
  dateFrom?: string,
  dateTo?: string,
  page?: number,
  region?: string[],
  search?: string,
  target?: string[],
}
